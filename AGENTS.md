# エージェント向け作業ガイド

このファイルは、AI エージェントにプロジェクト内での基本的な作業方針と、外部ツールを安全に利用するための条件を伝えます。

## プロジェクト概要

- Spring Boot と Gradle を使用したサンプルプロジェクトです。
- Java 21 を使用します。
- Gradle は、プロジェクトに含まれる `./gradlew` を使用します。
- サンプルプロジェクトであっても、ツール運用上は商用の非公開リポジトリとして扱います。

## 作業時のルール

- 変更する前に、関係するファイルを確認してください。
- 依頼された範囲で、できるだけ小さな変更を行ってください。
- 関係のないファイルや既存の変更は、そのまま残してください。
- `build/` や `.gradle/` などの生成ファイルは編集しないでください。

## 機密情報と外部ツール

- ソースコード、未コミットの差分、設定、非公開リポジトリ名、これらを含むプロンプトやログを機密情報として扱ってください。
- 外部ツール、MCP サーバー、Agent Plugin、App、GitHub App などを導入または有効化する前に、送信先、送信される入力、認証と権限、保存期間、学習を含む二次利用の有無を確認してください。組織で承認された条件を満たす場合だけ利用してください。
- このリポジトリの接続、インデックス作成、アップロード、または機密情報の送信は、利用者の依頼と組織の承認を確認できる場合に限ります。確認できない場合は実行せず、利用者へ確認してください。
- ツールは既定拒否と最小権限を基本とし、必要な読み取りツールと対象だけを許可してください。未知のツールを既定で許可する設定や、不要な書き込み権限は使用しないでください。
- 必要な制限を設定ファイルで強制できない接続は、`mcp.json` など複数クライアントが読み込む共有設定へ追加しないでください。
- API キー、アクセストークン、パスワードなどの秘密情報は、リポジトリ、プロンプト、ログへ記録せず、環境変数または承認済みのシークレット管理機構を使用してください。
- 公開情報を外部サービスで調査する場合も、必要最小限の公開識別子だけを送信し、このリポジトリのコード、差分、質問文、ログを入力しないでください。
- DeepWiki は Codex から公開 OSS の Wiki を閲覧する用途だけに限定します。許可するツールは `read_wiki_structure` と `read_wiki_contents` だけとし、`ask_question`、非公開リポジトリの解析、Devin API キー、Devin.ai の GitHub App は使用しないでください。DeepWiki への通信自体が認められない環境では、`.codex/config.toml` の `deepwiki-project` を無効にしてください。

## 実装方針

- KISS の原則を基本とし、要件を満たす最も単純で分かりやすい設計と実装を選んでください。
- SOLID 原則は設計上の指針として考慮しつつ、将来の可能性を見越した抽象化や拡張は行わず、YAGNI の原則を優先してください。
- 不要なレイヤー、クラス、設定、汎用化を増やさず、オーバーエンジニアリングを避けてください。
- 過剰な防御的プログラミングに頼るのではなく、事前条件・事後条件・不変条件を明確にする契約による設計を重視してください。
- コードは人間が無理なく理解し、変更やレビューを行えることを重視し、意図が明確に伝わる読みやすい実装にしてください。

## 確認コマンド

必要に応じて、次のコマンドを使用します。

~~~bash
./gradlew test
./gradlew check
~~~

ドキュメントだけを変更した場合は、リンクやファイル名を確認してください。

## ドキュメント

- ドキュメントは日本語で記述してください。
- コマンドやファイルパスは、実際のプロジェクト構成と合わせてください。

## 作業完了時

- 変更内容を確認してください。
- 実行したテストや、確認できなかった項目を簡単に報告してください。

## 備忘

- 将来的にはSpring Boot + Flyway + jOOQの構成にしたい
